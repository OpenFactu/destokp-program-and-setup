{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos del asistente: sólo los comandos propios y abrir URLs en el navegador.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
